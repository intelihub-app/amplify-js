//npm.pkg.github.com/:_authToken=${GITHUB_TOKEN}
# Authentication is expected via your user-level ~/.npmrc or environment variables.
# For local use, you can run:
#   npm login --registry=https://npm.pkg.github.com --scope=@intelihub-app
# Or set an environment variable and rely on env interpolation:
#   export GITHUB_TOKEN=ghp_your_token_here
#   echo "//npm.pkg.github.com/:_authToken=${GITHUB_TOKEN}" >> ~/.npmrc
